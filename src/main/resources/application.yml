spring:
  profiles:
    active: m1

# 文档块
---
spring:
  profiles: m1
server:
  port: 8081
---
spring:
  profiles: m2
server:
  port: 8082
---
spring:
  profiles: m3
server:
  port: 8083

# 还可以使用命令行
# java -jar target/demo-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev

# 虚拟机参数
# -Dspring.profiles.active=dev

# spring boot 配置文件的加载位置和优先级顺序 由高到低
# /config/
# /
# classpath:/config/
# classpath:/   classpath就是resources文件夹idea默认的位置

# 通过命令行参数spring.config.location改变默认的配置文件位置